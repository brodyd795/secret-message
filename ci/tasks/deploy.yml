---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
  - name: master

run:
  path: /bin/sh
  args: ['master/ci/scripts/deploy.sh']

params:
  environment:
  deploy_webhook_password:
  project:
